# `saas-web-app-astro` template

Astro SaaS starter manifest. Targets Netlify by default (Astro's
`@astrojs/netlify` adapter).

## Change before shipping

1. `metadata.author` and `metadata.source.url`.
2. Switch to `platforms.vercel` if you prefer Vercel; replace the
   adapter accordingly.
