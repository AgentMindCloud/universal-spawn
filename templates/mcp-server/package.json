{
  "name": "your-mcp-server",
  "version": "0.1.0",
  "description": "Replace with a one-line MCP server description.",
  "type": "module",
  "bin": { "your-mcp-server": "./dist/index.js" },
  "files": ["dist/", "README.md"],
  "engines": { "node": ">=20" },
  "license": "Apache-2.0",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "scripts": {
    "build": "tsc -p .",
    "start": "node dist/index.js"
  }
}
