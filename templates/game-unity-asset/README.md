# `game-unity-asset` template

Unity Asset Store package — drop into an existing URP project.

Change `platforms.unity.min_editor` if you target a different editor
version, plus `metadata.author` and source URL.
